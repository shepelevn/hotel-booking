TODOS:

    - Add authorization
    - Add routes and controllers

    - Add mailing functionality
    - Add admin panel

    - Add '/' route with 'index' layout

    - Run phpstan
    - Check todos
    - Delete var_dump's, dd's and dump's
